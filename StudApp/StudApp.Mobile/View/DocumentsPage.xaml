<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:StudApp.Mobile.ViewModel"
             x:Class="StudApp.Mobile.View.DocumentsPage"
             x:DataType="viewmodels:DocumentsViewModel"
             Title="Документы"
             BackgroundColor="#F8F9FA">

    <Grid RowDefinitions="Auto,*" Padding="0,0,0,20">

        <!-- Заголовок: Назад + Документы -->
        <Grid Grid.Row="0" 
              ColumnDefinitions="Auto,*,Auto" 
              HeightRequest="70"
              BackgroundColor="White"
              Margin="20,20,20,10">

            <Button Grid.Column="0"
                    Text="← Назад"
                    Command="{Binding GoBackCommand}"
                    BackgroundColor="#E2E8F0"
                    TextColor="#4A5568"
                    CornerRadius="12"
                    Padding="16,0"
                    FontSize="16"
                    FontAttributes="Bold"
                    VerticalOptions="Center"/>

            <Label Grid.Column="1"
                   Text="Документы"
                   FontSize="28"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   VerticalOptions="Center"
                   TextColor="#2D3748"/>

        </Grid>

        <!-- Список документов из БД (Telegram-style) -->
        <ScrollView Grid.Row="1" Margin="20,0">
            <CollectionView ItemsSource="{Binding Files}"
                           SelectionMode="None"
                           Margin="0,0,0,20">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="viewmodels:DocumentItem">
                        <Grid ColumnDefinitions="70,*,Auto"
                              HeightRequest="85"
                              Margin="0,6"
                              BackgroundColor="White"
                              Stroke="#E2E8F0"
                              StrokeThickness="1"
                              StrokeShape="RoundRectangle 16"
                              Padding="1">

                            <!-- Иконка PDF (Unicode — без файлов!) -->
                            <Grid Grid.Column="0">
                                <Frame BackgroundColor="#E53E3E"
                                       CornerRadius="14"
                                       WidthRequest="60"
                                       HeightRequest="60"
                                       Margin="8,12,0,12"
                                       HasShadow="True"/>
                                <Label Text="📄"
                                       FontSize="32"
                                       HorizontalOptions="Center"
                                       VerticalOptions="Center"
                                       TextColor="White"/>
                            </Grid>

                            <!-- Информация о файле -->
                            <VerticalStackLayout Grid.Column="1"
                                                Margin="16,12,12,12"
                                                Spacing="4">
                                <Label Text="{Binding Name}"
                                       FontSize="17"
                                       FontAttributes="Bold"
                                       LineBreakMode="TailTruncation"
                                       MaxLines="2"
                                       TextColor="#2D3748"/>
                                <Label Text="{Binding Size}"
                                       FontSize="14"
                                       TextColor="#718096"
                                       Opacity="0.8"/>
                            </VerticalStackLayout>

                            <!-- Кнопка СКАЧАТЬ -->
                            <Button Grid.Column="2"
                                    Text="Скачать"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DownloadFileCommand}"
                                    CommandParameter="{Binding .}"
                                    BackgroundColor="#48BB78"
                                    TextColor="White"
                                    CornerRadius="12"
                                    Padding="16,12"
                                    FontSize="14"
                                    FontAttributes="Bold"
                                    VerticalOptions="Center"
                                    Margin="8,0,8,0"/>

                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </ScrollView>

    </Grid>
</ContentPage>
