<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HabitTracker.Views.HabitListPage"
             Title="Мои Привычки">

    <VerticalStackLayout Padding="20" Spacing="10">
        <Label Text="Мои привычки" FontSize="28" FontAttributes="Bold" />

        <Button Text="+ Добавить" 
                Command="{Binding AddHabitCommand}"
                BackgroundColor="#007AFF"
                TextColor="White" />

        <ActivityIndicator IsRunning="{Binding IsLoading}" 
                          IsVisible="{Binding IsLoading}" />

        <CollectionView ItemsSource="{Binding Habits}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Padding="15" Margin="0,5" BackgroundColor="#F5F5F5">
                        <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" />
                        <Label Text="{Binding Description}" FontSize="14" />

                        <Button Text="Удалить"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteHabitCommand}"
                                CommandParameter="{Binding Id}"
                                BackgroundColor="#FF3B30"
                                TextColor="White"
                                Margin="0,10,0,0" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>

</ContentPage>
